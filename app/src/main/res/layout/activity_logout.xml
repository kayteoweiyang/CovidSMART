<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".Logout">

    <Button
        android:id="@+id/loginbtnLogout"
        android:layout_width="208dp"
        android:layout_height="52dp"
        android:layout_marginTop="20dp"
        android:fontFamily="@font/aldrich"
        android:text="Click to Login"
        app:layout_constraintEnd_toEndOf="@+id/text2Logout"
        app:layout_constraintHorizontal_bias="0.505"
        app:layout_constraintStart_toStartOf="@+id/text2Logout"
        app:layout_constraintTop_toBottomOf="@+id/text2Logout" />

    <TextView
        android:id="@+id/text2Logout"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="14dp"
        android:fontFamily="@font/aldrich"
        android:shadowColor="@color/black"
        android:text="Goodbye"
        android:textColor="@color/black"
        android:textSize="18sp"
        app:layout_constraintEnd_toEndOf="@+id/textLogout"
        app:layout_constraintStart_toStartOf="@+id/textLogout"
        app:layout_constraintTop_toBottomOf="@+id/textLogout" />

    <TextView
        android:id="@+id/textLogout"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="24dp"
        android:fontFamily="@font/aldrich"
        android:shadowColor="@color/black"
        android:text="You have successfully logout"
        android:textColor="@color/black"
        android:textSize="18sp"
        app:layout_constraintEnd_toEndOf="@+id/imgLogout"
        app:layout_constraintStart_toStartOf="@+id/imgLogout"
        app:layout_constraintTop_toBottomOf="@+id/imgLogout" />

    <ImageView
        android:id="@+id/imgLogout"
        android:layout_width="202dp"
        android:layout_height="226dp"
        android:layout_marginTop="144dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.497"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/logo" />

</androidx.constraintlayout.widget.ConstraintLayout>